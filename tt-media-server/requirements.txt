aiohttp
asyncio
colorama
fastapi
faster_fifo
httpx
huggingface_hub
imageio-ffmpeg
loguru
prometheus-client
prometheus-fastapi-instrumentator
psutil
pydantic-settings
pytest
pytest-asyncio
python-multipart
silero_vad
tqdm
uvicorn
whisperx==3.4.3
# Speaker diarization (MODEL_SERVICE=diarization) loads community-1, which
# needs pyannote.audio 4.x. whisperx pins pyannote-audio<4, so uv-overrides.txt
# resolves the conflict; see the note there.
pyannote.audio>=4.0,<5
soundfile
# tt-metal's venv ships a torchvision built against the old torch pin; it must be
# reinstalled together with the newer torch (see uv-overrides.txt) or importing it
# fails with a circular-import error from its C extension.
torchvision
torch==2.7.1+cpu
torchaudio==2.7.1+cpu
requests
num2words


--extra-index-url https://download.pytorch.org/whl/cpu
